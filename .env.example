# PostgreSQL Database Configuration
# Copy this file to .env and update with your own values
# NEVER commit .env to version control!

# Database credentials
POSTGRES_DB=wiki
POSTGRES_USER=wikijs
POSTGRES_PASSWORD=change_this_to_a_strong_random_password

# Wiki.js Database Connection
DB_TYPE=postgres
DB_HOST=db
DB_PORT=5432
DB_USER=wikijs
DB_PASS=change_this_to_a_strong_random_password
DB_NAME=wiki

# Generate a strong password using:
# openssl rand -base64 32
